/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        remotePatterns:[
            {hostname: "adorable-sardine-335.convex.cloud"}
        ],
    },
};

export default nextConfig;
